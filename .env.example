# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# WooCommerce / FooEvents (read-only)
WC_CONSUMER_KEY=
WC_CONSUMER_SECRET=

# Outbound email (Resend preferred for blasts)
RESEND_API_KEY=

# GoDaddy SMTP (confirmation emails only — Resend preferred)
SMTP_USER=support@youngatheart.co.za
SMTP_PASS=

# Inbound mail fetch (mail-fetcher cron at /api/cron/mail-fetcher)
IMAP_HOST=imap.secureserver.net
IMAP_PORT=993
IMAP_USER=support@youngatheart.co.za
IMAP_PASS=

# Vercel cron auth (optional). When set, mail-fetcher requires
# Authorization: Bearer ${CRON_SECRET}
CRON_SECRET=
